use_frameworks!

platform :ios, '10.0'

target 'relasebird-ios-sdk_Example' do
  pod 'relasebird-ios-sdk', :path => '../'

  target 'relasebird-ios-sdk_Tests' do
    inherit! :search_paths

    
  end
end
